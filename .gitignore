# Zig
.zig-cache/
zig-out/
zig-pkg/

# Bun
node_modules/

# Coverage — generated by `bun test --coverage`, never committed
coverage/

# Agent scratch (not committed)
!.claude/
.claude/*
!.claude/hooks/
!.claude/hooks/**
!.claude/skills/
!.claude/skills/**
.agent/scratch/
.claude/logs/
.claude/audit/

# Runtime state — Cipher session DBs, never committed anywhere
data/cipher-sessions*

# Fuzz outputs (corpora are versioned explicitly via .gitkeep)
fuzz/out/

# OS
.DS_Store

# Editor
.idea/
.vscode/
.cmux/

# Build report (scratch only)
*.log
